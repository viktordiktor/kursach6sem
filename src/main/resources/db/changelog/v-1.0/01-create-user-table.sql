CREATE TABLE user (
                      id BIGINT PRIMARY KEY AUTO_INCREMENT,
                      phone VARCHAR(255),
                      password VARCHAR(255),
                      role VARCHAR(255)
);